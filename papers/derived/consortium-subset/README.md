# Consortium Subset

This folder holds the `654`-paper candidate corpus promoted from the broader literature atlas.

`manifest.csv` is the first narrowed working set for downstream full-text parsing and mechanistic review.

It is broader than the final `215 relevant` papers and should be treated as a candidate pool rather than a finished corpus.
